package FunctionInterface.param;


public class action {
    /**
     * 在此处，我只是用一个简单的例子做个示范。实际上对于该例子来讲，就是把接口的实现放到了测试类里。
     * 实际应用最简单的例子就是Runnable接口。
     * @param click
     */
    public void Click(click click){
        click.fun();
    }
}
