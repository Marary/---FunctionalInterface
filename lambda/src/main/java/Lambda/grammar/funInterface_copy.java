package Lambda.grammar;

/**
 * This interface is similar to the funInterface interface,
 * except that it has only one parameter and no return value.
 * 此接口类似于funInterface接口，只不过只有一个参数，没有返回值。
 */
public interface funInterface_copy {
    void fun(int a);
}
